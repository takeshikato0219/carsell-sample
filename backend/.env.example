# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=katomo
DATABASE_PASSWORD=katomo_dev_password
DATABASE_NAME=katomo_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# API
PORT=4000
NODE_ENV=development

# File Storage (MinIO/S3)
STORAGE_ENDPOINT=localhost
STORAGE_PORT=9000
STORAGE_USE_SSL=false
STORAGE_ACCESS_KEY=katomo
STORAGE_SECRET_KEY=katomo_minio_password
STORAGE_BUCKET=katomo-files

# OpenAI (for AI features)
OPENAI_API_KEY=sk-your-openai-api-key

# Email (SendGrid/Resend)
SENDGRID_API_KEY=SG.your-sendgrid-api-key
EMAIL_FROM=noreply@katomo.com

# OCR (Google Cloud Vision)
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
