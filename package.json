{
  "name": "carsell-sample",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/main.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
